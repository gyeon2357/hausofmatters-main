---
import Cross from "../assets/images/cross.svg";
---

<dialog id="search-dialog">
 <form id="search-form">
  <button id="close-dialog" type="button" aria-label="Close search">
   <Cross width={16} height={16} />
  </button>

  <div class="search-input-row">
   <label for="search-input" class="sr-only">Search:</label>
   <input
    aria-label="search"
    type="text"
    id="search-input"
    placeholder="검색어를 입력하세요"
    maxlength="30"
    autocomplete="off"
    autocorrect="off"
    spellcheck="false"
   />
   <button type="submit" id="search-submit">Search</button>
  </div>

  <p id="search-hint" class="search-hint">아티스트 · 앨범 · 인터뷰 · 공연정보</p>
 </form>
</dialog>

<style>
 dialog {
  border: 0;
  padding: 2.5rem 3rem 3rem;

  background: var(--color-bg);
  color: var(--color-text);
  position: fixed;
  top: 33.33%;
  left: 50%;
  transform: translate(-50%, -50%);
  margin: 0;
 }

 dialog::backdrop {
  background: color-mix(in srgb, var(--color-text) 10%, transparent);
  backdrop-filter: blur(4px);
 }

 #close-dialog {
  display: flex;
  align-items: center;

  position: absolute;
  top: 0.75rem;
  right: 0.75rem;
  background: none;
  border: none;
  color: inherit;
  cursor: pointer;
  opacity: 0.8;
  transition: opacity 0.25s;
  padding: 0.25rem;
 }
 #close-dialog:hover {
  opacity: 1;
 }

 .search-input-row {
  display: flex;
  align-items: center;
  gap: 0.5rem;
  border-bottom: 1px solid color-mix(in srgb, var(--color-text) 40%, transparent);
  padding-bottom: 0.5rem;
  margin-top: 1rem;
 }

 input {
  background: none;
  flex: 1;

  border: 0;
  color: inherit;

  padding: 0.25rem 0;
  font-family: inherit;
  font-size: 1rem;
 }
 input:focus {
  outline: none;
 }
 input::placeholder {
  color: inherit;
  opacity: 0.3;
 }

 #search-submit {
  background: none;
  border: none;
  color: inherit;
  font-size: var(--font-size-s, 0.8rem);

  cursor: pointer;
  padding: 0 0.25rem;
  opacity: 0.6;
  transition: opacity 0.15s;
  font-family: inherit;
  flex-shrink: 0;
  white-space: nowrap;
 }
 #search-submit.has-results {
  opacity: 1;
 }
 #search-submit:hover {
  opacity: 1;
 }

 .search-hint {
  font-size: var(--font-size-s, 0.75rem);
  opacity: 0.75;
  margin: 0.5rem 0 0;
  min-height: 1.4em;
 }
</style>
